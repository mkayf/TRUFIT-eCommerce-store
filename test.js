// const formattedNumber = new Intl.NumberFormat('en-PK', { style: 'currency', currency: 'PKR' }).format(22300);

// // Convert the formatted number back to a number
// const numberValue = parseFloat(formattedNumber.replace(/[^\d.-]/g, ''));

// // Now you can perform mathematical operations on the number
// const result = numberValue;
// console.log(typeof(result)); // Outputs: 27300


